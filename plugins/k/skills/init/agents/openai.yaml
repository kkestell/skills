interface:
  display_name: "Workspace Init"
  short_description: "Initialize .k/ directory for k workflow"
  default_prompt: "Use $init to set up the .k/ directory structure in this repository."

policy:
  allow_implicit_invocation: false

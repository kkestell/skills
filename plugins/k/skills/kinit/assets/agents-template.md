# AGENTS.md

THIS FILE MUST BE KEPT UP TO DATE AT ALL TIMES

{2–3 sentence project description}

## Tech Stack

- **Language(s):** {e.g., Rust (edition 2024), TypeScript 5.x}
- **Frameworks:** {e.g., Actix-web, React 19, Next.js 15 — omit if none}
- **Build system:** {e.g., Cargo, npm, Gradle}

{If the project has multiple distinct tech stacks (e.g., a Go backend + TypeScript frontend), list each on its own line. If the entire project shares one stack, keep it brief.}

## Codebase Map

{A short description of each major part of the codebase — what it is, what it does, and where it lives. Organize this however makes sense for the project: by directory, by layer, by domain area. One line per entry is ideal. Only break into subsections if the project has genuinely separate deployable units with different tech stacks (e.g., "### Client" / "### Server").}

- `{path/}` — {what this part does}

## Commands

{Project-level commands. If the entire project shares one build system, list commands once here. If distinct parts of the project have different commands (e.g., a client and server with different toolchains), use subsections.}

- Build: `{command}`
- Run: `{command}`
- Test: `{command}`
- Lint: `{command}`
- Format: `{command}`

## Project Rules

<!-- Add project-specific rules, conventions, and preferences here. -->
